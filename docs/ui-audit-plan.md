# Визуальный аудит интерфейса и план работ

Аудит проведён 25.09.2026 на ширине экрана 390 px: экраны покупателя (главная, меню, каталог,
карточка товара, корзина, оформление, избранное, профиль) и все 10 вкладок панели администратора
на эмуляторах Firebase. Дополнительно проведён разбор кода: тени, цвета, кнопки, тексты.
Фотографии товаров в тестовой среде не загружались, поэтому композиция с реальными снимками
не оценивалась.

## Этап 0. Критичное — сделано в этом PR

| Проблема | Что сделано |
|---|---|
| Каждому гостю по умолчанию показывался профиль администратора (имя, email, телефон, адрес офиса), и он же подставлялся в форму заказа | Профиль по умолчанию пустой (`GUEST_USER_PROFILE`). Сохранённый в браузерах кэш со старыми данными администратора удаляется при загрузке |
| В форме заказа и окне адреса подставлялись выдуманные данные («Александр Новиков», `alex@manstyle.ru`, «ул. Ленина, 10», индекс 101000) | Поля пустые. Для курьера и Почты России улица стала обязательной |
| Демо-мерки тела (184 см / 94 кг) показывались как мерки пользователя | Показывается пустое состояние с подсказкой заполнить мерки |
| Противоречия: возврат 14 дней (главная, FAQ, бот) против 30 дней (карточка товара); бесплатная доставка от 5 000 ₽ против 15 000 ₽ (FAQ); в FAQ «два способа доставки» и Boxberry, которого нет | Срок возврата и порог доставки берутся из настроек магазина (`returnPeriodDays`, `freeDeliveryThreshold`). Тексты про доставку соответствуют реальному списку способов |
| Контакты-заглушки (8 (800) 555-35-35, +7 (495) 123-45-67, +7 (999) 000-00-00, `concierge@manstyle.ru`) и выдуманные реквизиты юрлица (ООО «МЭНСТАЙЛ РУС», ИНН, ОГРН, счёт) показывались покупателям и печатались в счёте | Значения по умолчанию пустые. `src/utils/storeContacts.ts` не показывает старые демо-значения, даже если они уже сохранены в Firestore. Пустые контакты скрываются, для реквизитов показано пояснение |
| Покупатель видел технические детали и админку: «Firebase Cloud / Firestore активен», «Войти через Google (Firebase Auth)», «Профиль & Админка», пункт «Панель администратора», «Выйти из аккаунта» у гостя, «Артикул SKU», внутренний ID вместо артикула | Блок называется «Аккаунт», технические детали видит только администратор. Пункт админки показывается только верифицированному администратору. Выход — только для вошедших. Внутренний ID убран |
| Бот обещал промокоды FIX500 и MANSTYLE10, которых может не быть в базе; прямые кавычки и «->» | Ответы бота переписаны без конкретных кодов, с «ёлочками» и «→» |

**Нужно сделать владельцу:** заполнить реальные контакты и реквизиты в «Панель администратора → Витрина».
Пока они пустые, покупатели видят только чат поддержки.

## Этапы 1–2. Кнопки, состояния и тени — сделано

Правила дизайн-системы (классы в `src/index.css`):

| Что | Класс | Когда |
|---|---|---|
| Главное действие | `neu-button-accent` | Одно на экран или окно: «Оформить заказ», «Сохранить», «Добавить товар» |
| Обычное действие | `neu-button` | Всё остальное. Действия в строках списков — `neu-button` с сиреневым текстом |
| Опасное действие | `neu-button-danger` | Удаление, очистка, выход. Не заливается цветом. Удаление всегда через `ConfirmDialog` |
| Выбранный вариант | `neu-pill-active` | Вкладка, сегмент, фильтр, способ оплаты, пункт меню: вдавлен, сиреневый текст. Полоса переключателя выпуклая (`neu-flat-sm`) |
| Бейдж, отмеченная галочка, включённый переключатель | `neu-fill-accent` | Не кнопки: без реакции на наведение и нажатие |
| Своё сообщение в чате | `neu-bubble-own` | |
| Статус, ошибка, предупреждение | `text-/bg-/border-success`, `warning`, `danger`; светлая подложка — `bg-*-soft` | Токены в `@theme` (`src/index.css`). Яркие emerald, rose, amber не используются |

Что сделано:

- Тёмная `neu-button-primary` удалена. «Оформить заказ» в корзине и кнопки в пустых экранах стали
  сиреневыми, как «Подтвердить заказ». Сообщения в чатах получили отдельный класс.
- Выбранные варианты (≈90 мест) и активные вкладки выглядят одинаково: вдавлены, сиреневый текст.
  Раньше было четыре разных вида, в том числе заливка как у главной кнопки.
- «Редактировать» в каталоге админки, «+» на карточках товара, «Детали» в заказах и похожие
  повторяющиеся действия — вторичные кнопки.
- Удаление, очистка и выход — `neu-button-danger` (#7E525E). «Очистить статистику базы» убрана из
  шапки аналитики (осталась внизу, в блоке отчётов), кнопка мгновенной очистки корзины убрана из шапки.
  Подтверждение добавлено там, где его не было: баннеры, промокоды, шаблоны ответов, история чата,
  массовое удаление товаров, адреса и карты в профиле.
- Все тени в `index.css` заданы через переменные: цвета `--neu-shadow-dark/-light`, уровни
  `--neu-raised-sm/md/lg`, `--neu-inset-sm/lg` и тени для модальных окон, фото и залитых кнопок.
- Убраны 137 теней Tailwind, которые перекрывались неоморфными классами, и плоские тени без основы.
  Удалены неиспользуемые классы и дубликаты (`neu-card`, `neu-accent-button`, `neu-success-button`,
  `neu-convex`, `neu-concave`, `neu-flat-lg`, `neu-badge`, `neu-modal-card` и др.) и глобальный стиль
  `[aria-selected="true"]`.
- Сиреневый при наведении — один вариант (#4F5DC0) вместо шести.
- Цвета статусов переведены на токены `success` (#3B6652), `warning` (#6F5B31) и `danger` (#7E525E)
  с подложками `*-soft`. Около 700 мест вместо ярких emerald, rose и amber и прямых hex-значений.
  Текст статусов проходит 4.5:1 и на фоне, и на подложке. Кнопка успеха больше не ярко-зелёная.
  Графики аналитики, карта доставки, PDF-отчёт и звёзды рейтинга оставлены яркими: там цвет
  различает данные.

**Найдено по ходу и исправлено в #20:** корзина нового посетителя была заполнена демо-товарами,
кнопка «Повторить заказ» ничего не добавляла в корзину.

## Этап 3. Читаемость и доступность — сделано

- **Контраст WCAG AA (4.5:1).** Вторичный текст #5C6B80 → #4E5C70 (5.5:1, ~1 080 мест). Сиреневый текст
  #5F6ED0 → #4B59BB (4.95:1 на фоне и 4.56:1 на кнопке, ~920 мест); #4F5DC0 из плана на кнопках давал
  только 4.29:1. Сиреневый при наведении — #3F4BA6. Подсказки в полях → #56647A (4.9:1) без прозрачности.
  Белый текст на сиреневой кнопке: градиент #5767CC → #4553B8 (не меньше 4.98:1, было 3.76:1).
  Для текста есть переменная `--accent-text`, `--accent-periwinkle` остаётся для заливок и рамок.
- **Шрифт не меньше 11 px:** `text-[8px]`, `[9px]`, `[10px]` заменены на `text-[11px]` (~760 мест).
- **Видимый фокус с клавиатуры:** сиреневая обводка через `:focus-visible` для кнопок, ссылок и полей
  (`src/index.css`). Убраны глобальный `outline: none` у кнопок и 199 классов `outline-none`.
- **Подписи для экранных чтецов:** `aria-label` у 200 кнопок из одной иконки (было 19): из `title`,
  «Закрыть» у крестиков, «Уменьшить/Увеличить количество», «Показать/Скрыть пароль», «Оценка N из 5» и др.

## Этап 4. Тексты и типографика — сделано

- **Название магазина — Wasat Shop.** Один источник: `getStoreName` в `src/utils/storeContacts.ts`
  (сохранённое название из «Витрины», старое MANSTYLE/ManStyle и пустое → «Wasat Shop»). Шапка, меню,
  главная, FAQ, бот, трекинг, счёт, PDF, CSV, уведомления и заголовок вкладки («Wasat Shop — мужская
  одежда») берут его оттуда. Тексты из базы (витрина, доставка, ПВЗ, баннеры) покупатель видит с
  новым названием ещё до миграции (`withStoreNameFields` в `App.tsx`).
- **Миграция данных.** В админке, «Витрина» → карточка «Название магазина в данных»: показывает, где в
  базе осталось MANSTYLE, и по кнопке заменяет его. Коды промокодов, ID и ссылки не меняются,
  демо-контакты шаблона очищаются.
- **Префикс WS-:** новые заказы (клиент и функция `placeOrder`), артикулы новых товаров и копий,
  номер счёта. Старые номера и артикулы остаются.
- Не менялись внутренние идентификаторы: ключи `localStorage` `manstyle_*`, ID базы Firestore,
  проект эмулятора `demo-manstyle`.
- **«&» → «и»** в 31 надписи («Склад и SKU», «Доставка и ПВЗ», «Клиенты и CRM»…).
- **«ё» → «е»** (46 мест): в коде, данных и ключах артикулов уже «е», поиск по материалам понимает оба
  варианта.
- **Шаги оформления** считаются по заполненным разделам (контакты → доставка → оплата), нажатие на
  шаг прокручивает к разделу. В шапке вместо «Шаг 1 из 4» — число товаров.
- **Обрезанные тексты:** названия способов доставки и ПВЗ при оформлении и подзаголовок меню
  переносятся на вторую строку, заголовок админки не обрезается.
- **Тексты для разработчиков убраны:** «Firestore», «Recharts», «Neumorphism», «(Intro)»,
  «Модальное окно • Вкладка 1», «Firebase Auth» → «Аккаунт Google».
- **Выдуманные контакты:** `support@manstyle-store.ru` → email из настроек (без него ссылка скрыта);
  «Позвонить курьеру» на +7 (916) 555-01-99 → «Позвонить в магазин» на телефон из настроек;
  курьер без выдуманной машины и номера. В чате поддержки убраны локальные «диалоги по заказам»
  (ответ в них никуда не уходил) и кнопка «Новый» с выдуманным клиентом. Имя, телефон и заказ в
  диалоге берутся из заказов этого покупателя, а не из последнего заказа магазина.
- Осталось владельцу: демо-пункты выдачи из `src/data/deliveryData.ts` с их телефонами заменить
  реальными в «Доставка и ПВЗ».

## Этап 5. Соответствие магазину мужской одежды — ~3–5 дней, нужен макет

1. **Акцентный цвет.** Сиреневый (#5F6ED0) вместе с серо-голубым фоном выглядит как
   финтех-приложение. Для премиальной мужской одежды рассмотреть графит (#2D3A4E, уже
   используется) как основной акцент и приглушённое золото (#8C733E) для «Премиум» и «Хит».
2. **Фото в карточках.** Увеличить фото (соотношение 3:4, почти без рамки), уменьшить толщину
   выпуклых рамок вокруг карточек. Неоморфизм оставить для элементов управления, а не для витрины.
3. **Шрифт.** Для заголовков — шрифт с характером (например, Manrope или Montserrat с
   кириллицей), для текста — системный.
4. Макет и согласование делаются до правки кода.

## Порядок и оценка

| # | Этап | Оценка | Зависимости |
|---|---|---|---|
| 0 | Критичное | сделано | — |
| 1 | Иерархия кнопок и состояния | сделано | — |
| 2 | Тени и токены | сделано | — |
| 3 | Читаемость и доступность | сделано | — |
| 4 | Тексты и типографика | сделано | — |
| 5 | Стиль бренда | 3–5 дней | макет и согласование; после этапов 1–3 |

Этапы 1–4 можно делать отдельными PR. Каждый проверяется скриншотами экранов до и после на
эмуляторах (`VITE_USE_EMULATORS=true`, см. README).
